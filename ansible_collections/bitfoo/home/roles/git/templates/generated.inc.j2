; Ansible managed
[user]
    email = "{{ git_user_mail }}"
    name = "{{ git_user_name }}"

{% for item in git_include_if %}
[includeIf "gitdir:{{ item.path }}"]
    path = "{{ xdg_dirs_config }}/git/includes/{{ item.path | hash('sha1') }}.inc"
{% endfor %}
